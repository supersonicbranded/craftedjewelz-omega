name: build

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: apps/web
        run: npm install

      - name: Build frontend
        working-directory: apps/web
        run: npm run build

      - name: Install ImageMagick
        run: sudo apt-get update && sudo apt-get install -y imagemagick icnsutils

      - name: Prepare Icons
        run: |
          mkdir -p build/icons
          convert assets/appicon2.png -resize 1024x1024 build/icons/craftedjewelz.icns
          convert assets/appicon2.png -resize 256x256 build/icons/craftedjewelz.ico
          cp assets/appicon2.png build/icons/craftedjewelz.png

      - name: Package & Publish
        working-directory: apps/web
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: npx electron-builder --publish always --x64
